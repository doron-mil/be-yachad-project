import { DynamicColContentDirective } from './dynamic-col-content.directive';

describe('DynamicColContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicColContentDirective();
    expect(directive).toBeTruthy();
  });
});
